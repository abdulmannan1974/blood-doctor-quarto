---
description: Start a live, auto-reloading Quarto preview.
argument-hint: "[file] [--port N]"
---
Start `quarto preview $ARGUMENTS`. Load `references/01-commands.md`.
Run it in the background, confirm the local URL returns HTTP 200, and tell the user the address.
Note that edits to source auto-rebuild. Stop with the returned server id when asked.
