---
description: Build a reveal.js presentation with executable code.
argument-hint: "[topic]"
---
Create a `revealjs` deck on **$ARGUMENTS**. Load `references/02-formats.md` (presentations) + `05-authoring.md`.
Include: title slide, incremental bullets, a two-column slide, an executable figure cell, and speaker notes (`::: {.notes}`).
Set `format: revealjs` with `slide-number`, `incremental`. Render and verify, then offer PDF export (`--to revealjs` then print, or `pptx`).
